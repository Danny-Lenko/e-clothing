import Directory from "../../components/directory/directory.component";

// TODO: Make page query categories from the backend

const Home = () => {
   return (  
      <Directory />
   );
}
 
export default Home;